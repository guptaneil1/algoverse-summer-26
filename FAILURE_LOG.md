# Failure Log

> Stage A completed on 2026-08-07, on the fourth attempt. `PC-2026-08-03-A` and `-B` record
> why the first attempt could not start; `-C` through `-G` record the defects and mistakes
> found while getting the run to execute; `-H` records what was lost after it finished.
> The completed run is written up in `docs/positive_control/report.md`.

Failures, null results, contradictory evidence, and protocol violations must be retained. They may not be deleted because they weaken the preferred conclusion.

## Entries

Newest first, except `PC-2026-08-03-A`, which is kept at the head as the opening entry.

| ID | Date | Stage | Run/claim | Failure | Evidence | Cause status | Resolution | Scientific consequence |
|---|---|---|---|---|---|---|---|---|
| `PC-2026-08-03-A` | 2026-08-03 | A | `positive_control_fully_synthetic_seed42`, `positive_control_human_mixed_seed42` | Neither arm could be launched: the Week 2 environment has no accelerator. | `capture_environment()` output recorded in `docs/benchmarks/khantushig_week2.md` §2 and `docs/positive_control/failure_report.md` §2.1: `accelerator: none_detected`, `torch: not_installed`, `nvidia-smi` absent, 4 vCPU / no CUDA device. Upstream `config/config.yaml` requires `cuda_device: 0`, `torch_dtype: bfloat16`. | **Infrastructure** — not implementation. No defect was found in the adapter, configs, or script; all four contract tests pass (158 passed, 1 skipped). | Rerun is permitted under the frozen rules once an accelerator host is provisioned (`PROTOCOL.md` rerun rule: infrastructure failures may be rerun unchanged). No configuration change is required or permitted as part of that rerun. | Stage A is untested, so **Stage B remains blocked** (`PROTOCOL.md` §4). No reproduction claim may be made in either direction. This is a blocked run, not a failed reproduction. |
| `PC-2026-08-07-H` | 2026-08-07 | A | Artifact retention for both completed arms | Both arms completed all 11 generations, but the model checkpoints and generated corpora — 42 artifacts — were lost before they could be copied out. Only the metrics files survived. | On return to the session `/kaggle/working` contained a single directory (`.virtual_documents`); every run artifact was gone. The metrics survived because an auto-push monitor had been committing each generation's `eval_results.json`, `train_results.json`, `all_results.json` and `artifact_record.json` to this branch as it completed — 22 of 22 generation-arm pairs are present, the last (`human_mixed gen 10`) committed 2026-08-07T04:12Z. | **Infrastructure** — the ephemeral Kaggle container was reclaimed at session end, as that platform documents. Not a defect in this repository or upstream, and not a protocol violation. Contributing factor in this repository's operating procedure: the save step that would have copied hashes and text samples out was designed to run at session end rather than incrementally alongside the metrics. | **No rerun.** Every frozen metric is retained and committed, so the scientific result is complete; rerunning could not recover the lost bytes, only produce different ones. The artifact records were deliberately **not** retro-edited to mark the artifacts `pruned`, because no prune occurred — `--prune-models` was never passed and `pruned: false` was true when the driver wrote it. The inventory of unretained artifacts is recorded separately in `docs/positive_control/measurements/artifact_retention.json`. | None on the reported result: all 22 `eval_results.json` files are present and are the source of every number in `expected_vs_observed.md` §3. **Recorded limitation:** the SHA-256 values taken at run time can no longer be verified against bytes, so `verify_recorded_hashes` cannot be run against this execution, and no sample of the degraded generation-10 text survives for qualitative inspection. Any future run on an ephemeral host must mirror artifacts incrementally, not at session end. |
| `PC-2026-08-06-G` | 2026-08-06 | A | Stage A launch, both arms in parallel | Both arms crashed after ~55 min in generation 1 with `torch.OutOfMemoryError` inside the ModernBERT detector, despite each arm needing only ~6-8 GiB on a 14.56 GiB card. | `nvidia-smi --query-compute-apps` showed both child processes on the same `gpu_uuid`, with the second GPU idle at 3 MiB. The OOM text is self-consistent with a shared device: `this process has 5.87 GiB in use. Process 2801 has 8.01 GiB in use`, 704 MiB free of 14.56 GiB. An isolated test confirmed `CUDA_VISIBLE_DEVICES` masking works correctly on this host, which ruled out the platform. | **Implementation defect, in this repository.** `run_step` in `scripts/run_positive_control_arm.py` built an environment dict setting `CUDA_VISIBLE_DEVICES`, `WANDB_DISABLED`, `WANDB_MODE` and `PYTHONPATH`, then called `subprocess.run(...)` **without `env=`**. Every child silently inherited the parent environment instead, so `--cuda-device` had never had any effect since the driver was written. | Fixed by passing `env=environment`. Four regression tests added that stub `subprocess.run` and assert the environment reaches the child; all four fail against the unfixed source and pass against the fix. Rerun permitted: no generation completed, and generation 0 was already recorded and untouched. | None on the frozen comparison — no arm produced a metric. Cost was roughly 90 minutes of wall clock. The defect also explains why the driver's own wandb settings appeared to do nothing in `PC-2026-08-05-E`: they were only ever effective because the notebook set the same variables in its own environment, which children inherit. |
| `PC-2026-08-06-F` | 2026-08-06 | A | `data.train_manifest_sha256` in both arm configs | The prepared `data/wikitext2/train.json` hashed to `f748ba2b…` in a fresh session, against the pinned `77557c85…` — the dataset was not reproducing. | Hash comparison run on Kaggle before generation 0 was recorded. Cause located in upstream source: `src/load_data.py:150` calls `classify_dataset(...)`, running the ModernBERT detector over the training split and saving the classified result as `train.json`. The file is therefore a function of the framework stack and of GPU inference, not of the raw dataset alone. The pinned value was produced under `transformers 5.15.0.dev0` / `datasets 5.0.0`; the new value under the frozen `transformers 4.48.3` / `datasets 3.2.0` (see `PC-2026-08-05-D`). | **Data/protocol — a provenance error in this repository, not an upstream defect.** The original pin was taken in an environment that cannot run upstream at all, so it never described a usable run. | Determinism verified before re-pinning, **within one session**: `load_data.py` was re-run in the same session on the same allocated GPU under the frozen environment and produced a byte-identical `train.json`, so the mismatch is consistent with the version change. `train_manifest_sha256` re-pinned to the value produced under the frozen stack, with the reason recorded in both configs. The superseded generation 0 from 2026-08-05 was trained on the old file and is discarded rather than reused. **Correction, same day:** this entry and `PROTOCOL.md` originally read the verification as showing the dataset is "reproducible within a fixed stack". It does not: the re-run shared a session and a physical accelerator with the original, so it establishes only within-session determinism. The narrower claim is what is now recorded in both places. | None on the frozen comparison, provided every generation is prepared and run under the same pinned stack, and any chain continued in a later session carries its prepared `train.json` forward instead of regenerating it. **Recorded limitation:** because dataset preparation includes detector inference, `train.json` is not guaranteed to be bit-identical across framework versions, across GPUs, or across sessions on the same stack. Any reproduction attempt elsewhere must re-pin and say so rather than assume the recorded hash transfers. |
| `PC-2026-08-05-E` | 2026-08-05 | A | Generation 0 training, `positive_control_fully_synthetic_seed42` | Training and evaluation completed, then the process died with `wandb.errors.errors.Error: You must call wandb.init() before wandb.log()` at `src/train.py:683`, exit 1 after 610.8 s. | Log at `runs/positive_control/fully_synthetic/stdout_stderr.log`. Cause is the defect already noted in the runbook §0.3: `main.py:25` guards initialisation with `if not bool(str(cfg.wandb_disabled))`, and `bool("False")` is `True`, so `wandb.init` is never called — while `train.py:683` calls `wandb.log` unconditionally. Setting `WANDB_DISABLED` and `WANDB_MODE=disabled` does not prevent it; wandb's pre-init stub raises regardless of mode. | **Implementation defect, in the upstream code.** Not a scientific failure: the crash is in dashboard reporting, after `trainer.save_model` (`train.py:652`), `save_metrics("train")` (`:662`) and `save_metrics("eval")` (`:681`). Everything remaining after line 683 is a second `wandb.log`, a `gt_cls_score` block that only prints counts, and `create_model_card`, which writes a cosmetic README. | **Generation 0's artifacts are retained, not recomputed.** `final_model/` (253.7 MB, 9 files), `train_results.json` (`train_runtime` 542.77 s) and `eval_results.json` (`perplexity` 30.1053, `eval_loss` 3.4047, `eval_accuracy` 0.38596, 4669 train samples) were all written before the crash and are complete. Fixed for subsequent generations by `write_wandb_shim()` in `scripts/run_positive_control_arm.py`, which performs the disabled-mode `wandb.init` upstream intended, via an auto-imported `sitecustomize.py` on the subprocess `PYTHONPATH`. Upstream is not edited. | None on the frozen comparison. wandb logging touches no scientific computation, and the shim only restores the `wandb_disabled=true` behaviour the configuration already requests. Recorded as an execution-mechanics deviation in `PROTOCOL.md`. |
| `PC-2026-08-05-D` | 2026-08-05 | A | Generation 0 training, `positive_control_fully_synthetic_seed42` | `src/train.py` aborted at import after 11.9 s: `ImportError: cannot import name 'send_example_telemetry' from 'transformers.utils'`. | Traceback at `src/train.py:48`, recorded in `runs/positive_control/fully_synthetic/stdout_stderr.log`. Environment at failure: `transformers 5.15.0.dev0`, `datasets 5.0.0`, `accelerate 1.13.0`, `torch 2.10.0+cu128`, Python 3.12, Tesla T4. Upstream `src/train.py:53` calls `check_min_version("4.48.0.dev0")`, and `send_example_telemetry` was removed in transformers v5. | **Infrastructure / environment resolution** — not a defect in upstream or in this repository. Upstream's `README.md` instructs `pip install git+https://github.com/huggingface/transformers`, which was correct when git main was `4.48.0.dev0` and now installs a later major version the code cannot run under. | Resolved by pinning the era-matched stack: `transformers==4.48.3`, `datasets==3.2.0`, `accelerate==1.2.1`, host `torch`. `PROTOCOL.md` already required the executing host to resolve and record a `transformers` version, since upstream pins none; this is that resolution. Rerun permitted — nothing had been recorded, so no generation was skipped or replaced. | None on the frozen comparison. The pinned versions become part of the run's identity and are recorded by `capture_environment()` in the run manifest; they must be quoted in the report alongside the model and dataset revisions. |
| `PC-2026-08-05-C` | 2026-08-05 | A | Compute measurement via upstream's `smoke_test=true` | `python main.py smoke_test=true ...` aborts after 3.9 s with `TypeError: expected str, bytes or os.PathLike object, not bool`. | Traceback at `main.py:55` (`subprocess.run`). Cause located in source at the pinned commit: `main.py:45` sets `low_cpu_mem_usage = str(cfg.low_cpu_mem_usage)`, but `main.py:51` — inside `if cfg.smoke_test:` — overwrites it with the bool `True`, which is then passed as a `subprocess.run` list element at `main.py:72`. Reproduced on Kaggle, Tesla T4, torch 2.10.0+cu128, transformers 5.0.0. | **Implementation defect, in the upstream code, not in this repository.** Reachable only through the `smoke_test` flag; the default path is unaffected because line 45's `str()` holds. | Not patched: upstream is the pinned reference and editing it would invalidate the reproduction it anchors. The measurement is taken instead by training generation 0 at full scale through `scripts/run_positive_control_arm.py --stop-after-generation 0`, which applies `str()` to every command value. See the runbook §0.7 and §5. | None on Stage A. The driver never sets `smoke_test` and constructs its own commands, so both arms are unaffected. The replacement measurement is at full scale rather than 100 samples, and its output is retained as Stage A's shared generation 0 instead of discarded. |
| `PC-2026-08-03-B` | 2026-08-03 | A | Expected-value extraction for `docs/positive_control/expected_vs_observed.md` §2.2 | The published numeric expected values could not be read, so the expected column is frozen qualitatively (ordering) but not numerically. | `docs/positive_control/failure_report.md` §2.2: the environment's network policy denies `CONNECT` to `aclanthology.org` and `huggingface.co` with HTTP 403; `github.com` is reachable, which is why the upstream commit and all upstream-derived settings could be pinned. | **Infrastructure** — network policy, not a protocol or implementation defect. | Resolved by extracting the values on a host with access and committing them **before** either arm runs, so the freeze remains provable by timestamp. Recorded as an open item rather than filled with an invented number. | The primary ordering criterion is unaffected and remains frozen. The 5% engineering tolerance cannot be applied until the published values exist, so a run completed before then could reach at most `valid_with_limitation`. |

## Entry rules

For every failure, record:

- exact run or claim identifier;
- code and configuration commit;
- manifest and log location;
- whether the cause is implementation, infrastructure, protocol, or scientific;
- evidence supporting that classification;
- whether rerunning is allowed under the frozen rules;
- effect on claims and future stages.

An unfavorable treatment result is not an implementation failure without independent evidence of a defect.
