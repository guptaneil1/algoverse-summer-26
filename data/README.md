# Data Policy

Raw datasets are not committed. This directory contains documentation, tiny fixtures, and immutable manifests only. Every real asset must record source, revision, license, preprocessing, stable IDs, hashes, partition, origin, mode definition, and tokenizer-dependent token counts.

Required mutually disjoint partitions: base human training, rescue candidates, generation prompts, validation, and final human test.
