# Data configurations

Neil records the final licensed domain, preprocessing, partition manifest paths/hashes, mode definition, and tokenizer-dependent token-counting rule here before the Week 2 freeze.
