torch>=2.1
transformers>=5.0   # v5 loads native dtype; train.py forces fp32 (see docstring)
datasets>=2.18
accelerate>=0.28
sentence-transformers>=2.6
nltk>=3.8
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
statsmodels>=0.14   # ANOVA + BH-FDR (Week 8 analysis)
matplotlib>=3.8
seaborn>=0.13
openai>=1.30        # LLM judge only; optional at pilot stage
pytest>=8.0
ruff>=0.4
