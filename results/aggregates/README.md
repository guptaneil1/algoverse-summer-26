# Generated Aggregates

This directory is empty until schema-valid primary or explicitly exploratory chain results are aggregated by a versioned command. Every aggregate records its input run IDs and hashes.
