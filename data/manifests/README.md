# Manifest Directory

Neil places versioned JSONL manifests here during Week 2. Primary manifests are immutable after the protocol freeze. A new version receives a new filename and hash; existing primary manifests are never silently edited.
