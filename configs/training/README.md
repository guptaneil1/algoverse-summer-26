# Training configurations

Khantushig records optimizer, schedule, batch size, gradient accumulation, sequence length, precision, checkpoint, and resume behavior here. All budget-matched conditions must consume equal realized total optimizer tokens.
